# Lifecycle schema validation

The clean-baseline rehearsal covers the durable ingestion-job, lease, cancellation, deletion-target, deletion-receipt, chat-session, chat-message, query-run, and query-event contracts used by the Preview gateway lifecycle increment.

This record does not claim that request-time processing is a fully asynchronous queue or Workflow consumer, and it does not authorize production changes.
